# Example secrets (do not commit actual secrets!)
apiVersion: v1
kind: Secret
metadata:
  name: enterprise-mcp-server-secrets
type: Opaque
stringData:
  API_KEY: "your-api-key-here"
  OAUTH_CLIENT_SECRET: "your-oauth-secret-here"
